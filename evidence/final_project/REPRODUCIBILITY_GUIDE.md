# Reproducibility Guide

1. Verify this Project Master against its `.sha256`.
2. Read `MASTER_STATUS.json`.
3. Verify every embedded branch Master against `REGISTRIES/BRANCH_MASTER_REGISTRY.csv`.
4. The exact canonical XBICA scientific closure is under `XBICA_CANONICAL_LINEAGE/`.
5. Direct project conclusions are under `DIRECT_PROJECT_CLOSURE/`.
6. Frozen ontology/edge rules are under `FROZEN_XBICA_CONTRACTS/`.
7. For full B08 byte-level evidence, verify the two external companion ZIPs and sidecars listed in `EXTERNAL_EVIDENCE/FINAL_EXTERNAL_EVIDENCE_SET.json`.
8. No scientific analysis is performed by Phase5 packaging.
